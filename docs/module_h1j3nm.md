
# Slefni Documentation

## Architecture
This module handles the core business logic for the Slefni domain.

## Setup
1. Install dependencies: `npm install`
2. Run tests: `npm test`

## Deployment
Docker containers are automatically built on push to main.
